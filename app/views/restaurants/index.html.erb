<h1>All restaurants</h1>

<ul>
  <% @restaurants.each do |restau| %>
    <li><%= restau.name %> <%= link_to "See details", restaurant_path(restau.id), class: "btn btn-primary"%></li>
  <% end %>
</ul>

<%= link_to "Add a restaurant", new_restaurant_path, class: "btn btn-primary"%>

